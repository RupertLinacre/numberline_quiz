import { defineConfig } from 'vite';

export default defineConfig({
    base: '/numberline_quiz/', // Important: leading and trailing slash!
    // If you have other build options, you can add them here
    // build: {
    //   outDir: 'dist', // This is the default, so not strictly necessary
    // }
});
