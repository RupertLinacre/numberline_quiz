export const config = {
    svgWidth: 800,
    svgHeight: 200,
    margins: { top: 50, right: 50, bottom: 70, left: 50 },
    initialDomain: [0, 2], // Default starting domain changed to [0, 2]
    debug: true, // For logging
    majorTickLength: 10,
    minorTickLength: 5,
    minZoomScale: 0.1,
    maxZoomScale: 100,
    answerTolerance: 0.01,
    // Add more as needed: zoomScaleExtent, tick lengths, etc.
};
